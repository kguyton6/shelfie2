// const  { create } = require ("domain");
let inventory = []
let id = 0



module.exports = {
    getAll: (req, res) => {
    // const db = req.body
    // dbInstance('db', db).then()
    res = res.status(200).send("I recommend it worked, woohoo") },
    
   getOne: ((req, res) => res = req.body)
   .then(req.body.id) 


}

